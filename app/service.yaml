apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  selector:
    app: myapp
  ports:
    - protocol: TCP
      port: 80        # Port inside the cluster
      targetPort: 80  # Container port
      nodePort: 30080 # Optional: custom NodePort (range 30000–32767)
  type: NodePort
